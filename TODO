1. Section blocks
2. Stats
3. Gadmins
   * Only allow gadmins to delete own users
4. Template users
5. User input validation (including filtering out unicode -- non ascii characters)
6. Group profiles are incomplete no save function, not being added when group created
7. Make it so user's don't have to log off then on when settings are changed
